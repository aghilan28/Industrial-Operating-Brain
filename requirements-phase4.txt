# Phase 4 — Telemetry Pipeline dependencies
# Install:  pip install -r requirements-phase4.txt
#
# SQLAlchemy 2.x async ORM + pooling
SQLAlchemy>=2.0.30
# Async driver for the dev/CI default engine (SQLite). For production
# PostgreSQL/TimescaleDB also install asyncpg and set DATABASE_URL.
aiosqlite>=0.19.0
asyncpg>=0.29.0
greenlet>=3.0.0
# Web framework + validation (already present in the platform; pinned for clarity)
fastapi>=0.110.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
# Used by validate_phase4.py to exercise the ASGI app in-process
httpx>=0.27.0
